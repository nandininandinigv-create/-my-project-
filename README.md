Added it — there's now a **"Vs. computer"** toggle above the board. Pick it and you play X against an unbeatable AI (minimax, so it'll always win or draw — good practice, never a pushover). "Two players" mode still works the same as before.

Just re-upload this `index.html` to your GitHub repo (overwrite the old one) and you're set.